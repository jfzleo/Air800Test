<modal class="modal" title="{{modalTitle}}" hidden="{{!modalShow}}" bindconfirm="modalChange" bindcancel="modalChange" backdrop="{{false}}">
  <view class='modal-content'> 
      <text>{{modalContent}}</text>
      <progress show-info percent="{{modalProgress}}" />
  </view>
</modal>